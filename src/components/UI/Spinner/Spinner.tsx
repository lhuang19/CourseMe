/**
 * 
 * Spinner to be displayed when pages are loading
 * 
 */

import classes from './Spinner.module.css';

const Spinner = () => <div className={classes.Spinner}></div>;

export default Spinner;
